PKG_CFLAGS=@PKG_CFLAGS@
PKG_CPPFLAGS=@PKG_CPPFLAGS@
PKG_LIBS=@PKG_LIBS@

$(SHLIB): @CSDP_TS@

Csdp.ts:
	@($(MAKE) -C Csdp/lib libsdp.a CC="$(CC)" CFLAGS="$(CFLAGS) $(CPICFLAGS) -DNOSHORTS -ICsdp/lib/include" AR=$(AR) RANLIB=$(RANLIB))
	touch $@
